# Ne JAMAIS committer de vraies clés. Les secrets passent par :
#   firebase functions:secrets:set STRIPE_SECRET_KEY
#   firebase functions:secrets:set STRIPE_WEBHOOK_SECRET
# (Dashboard Stripe → Développeurs → Clés API / Webhooks → Secret de signature)
